name: Build Fulbito (Mundial 2026)

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  # pull_request:
  #   branches: [ main, master ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build executable
      run: python build_app.py --variant fulbito

    - name: Upload Windows artifact
      uses: actions/upload-artifact@v4
      with:
        name: Fulbito-Windows
        path: dist/Fulbito/

  build-macos:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build executable
      run: python build_app.py --variant fulbito

    - name: Upload macOS artifact
      uses: actions/upload-artifact@v4
      with:
        name: Fulbito-macOS
        path: dist/Fulbito.app/

  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Syntax check core/
      run: python -m py_compile core/*.py

    - name: Syntax check fulbito variant
      run: python -m py_compile variants/fulbito/main.py variants/fulbito/config.py variants/fulbito/game_engine.py variants/fulbito/physics_world.py

  release:
    needs: [build-windows, build-macos, test]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
    - name: Download Windows artifact
      uses: actions/download-artifact@v4
      with:
        name: Fulbito-Windows
        path: ./windows

    - name: Download macOS artifact
      uses: actions/download-artifact@v4
      with:
        name: Fulbito-macOS
        path: ./macos

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          windows/
          macos/
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
